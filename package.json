{
  "private": true,
  "scripts": {
    "build": "turbo build --filter desktop",
    "dev": "turbo dev --concurrency 11 --filter !@repo/email --filter !docs --filter !desktop",
    "dev:desktop": "turbo dev --filter desktop",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@tauri-apps/cli": "^1.5.10",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "prettier-plugin-tailwindcss": "^0.5.10",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
